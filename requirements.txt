# Source of truth: pyproject.toml ([project] + [project.optional-dependencies].dev).
# Prefer: pip install -e ".[dev]"
# This file is kept for Dockerfile.test / tools that expect requirements.txt.
-e ".[dev]"
