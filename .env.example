DATABASE_URL=postgresql://gateway:gateway@localhost:5432/gateway
CONFIG_PATH=config/config.yaml
HOST=0.0.0.0
PORT=8000
LOG_LEVEL=info
# Обязателен при authz.mode=header_stub. Клиенты должны передавать то же значение в X-Gateway-Token.
GATEWAY_TRUST_TOKEN=demo-trust-token
